import React from 'react';
import SectionTitle from '../../../Components/SectionTitle/SectionTitle';

const ManagePersonalNotes = () => {
    return (
        <div>
           <SectionTitle header={'Manage Your Personal Notes'} subHeader={'You can manage personal notes by creating a simple React app with a text editor, save to local storage, and display them.'}></SectionTitle>

           {/* TODO: Kaj Baki */}
        </div>
    );
};

export default ManagePersonalNotes;