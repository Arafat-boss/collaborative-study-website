import React from 'react';
import SectionTitle from '../../../Components/SectionTitle/SectionTitle';

const ViewAllStudyMaterials = () => {
    return (
        <div>
            <SectionTitle header={'View all study materials'} subHeader={'You can manage personal notes by creating a simple React app with a text editor, save to local storage, and display them.'}></SectionTitle>
        </div>
    );
};

export default ViewAllStudyMaterials;