import SectionTitle from "../../../Components/SectionTitle/SectionTitle";

const ViewBookedSession = () => {
    return (
        <div>
            <SectionTitle header={'View booked session'} subHeader={'The "View Booked Sessions" feature allows users to manage and track their scheduled study sessions efficiently, ensuring better organization.'}></SectionTitle>

            {/* TODO: Kaj Baki Ase */}
        </div>
    );
};

export default ViewBookedSession;